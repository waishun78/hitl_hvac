# hitl_hvac
Holds the code for RARL-HIL

Training of the RARL model is in the RARL_training_main.ipynb (to get the .pth file)

Training of the DQN model is in the 2 Jupyter notebooks, it is also where the experiments are run (to get the .pth file)
